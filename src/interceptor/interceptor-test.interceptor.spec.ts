import { InterceptorTestInterceptor } from './interceptor-test.interceptor';

describe('InterceptorTestInterceptor', () => {
  it('should be defined', () => {
    expect(new InterceptorTestInterceptor()).toBeDefined();
  });
});
